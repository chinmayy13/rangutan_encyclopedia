I'm writing up the sulfur work on our sulfate reducing bioreactor and I need the XPS sulfur region and the column's operating record turned into something I can drop into the paper.

Everything is on the Desktop. `Bioreactor_XPS_Dataset.xlsx` holds the run. Sheet `Fig 3` is the sulfur 2p scan. The `Fig 1` and `Fig 2` sheets are the aqueous record against elapsed days, and the Read Me says which column is the feed and which is the treated effluent. Sheet `Table 1 -data` is the independent characterisation of that feed, given as a value plus its uncertainty. `Sulfur_Markers.csv` lists the reference binding energies for the sulfur species we expect. `Images_XPS.zip` has one sheet per species from the peak model, each printing that species' modelled peak height in counts per second. `Figure_Layout_Reference.png` is the blank two-panel figure our group uses, and `XPS_Processing_Notes.pdf` is our usual write-up of how we smooth and background-correct this kind of scan, match it to references, and handle censored or missing readings.

Start with the spectrum. Smooth the sulfur 2p scan with a Savitzky-Golay filter over a five-point window with a second-order polynomial — that's the window we always use for this region — handling the ends, the background and the reference matching the way `XPS_Processing_Notes.pdf` describes. For each reference in `Sulfur_Markers.csv`, if nothing qualifies within the notes' search radius, record the species as not resolved rather than forcing a match. For each species I want its assigned position, the offset from the reference, the amplitude the background-corrected trace reaches there, the modelled height printed on its sheet in `Images_XPS.zip`, and the difference between those last two (amplitude minus modelled height).

Then do the aqueous side. The column fails near the end of the run: take the failure point as the first sampling day where the treated effluent pH drops below 4 and stays below it, and split the record there, counting that day as the first failed one. For sulfate and for cadmium, iron, manganese and zinc, work out the mean feed and mean treated effluent concentration either side of that day, and calculate each period's removal from those means; treat censored and missing readings the way our processing notes describe, and put the `Table 1 -data` value alongside so I can see the feed held. Round everything the way those same notes do.

`Analysis_Record_Template.docx` is the record, but it is still last month's draft and it has been through review. Work from that file and give it back to me as `XPS_Analysis_Record.docx`, cleaned up: take the tracked edits, deal with every comment left on it and clear them out, and replace last month's numbers in both tables with this run's. Rewrite the method paragraph for what you actually ran and rewrite the conclusions to cover the largest offset and the species carrying it, the species where the modelled height and the measured amplitude disagree most in absolute terms, the ratio of the reduced sulfur species' combined modelled peak height to the oxidised species' combined modelled peak height, and what happens to the metals after the failure day. One of the comments is about the page: as it stands the performance table runs off the edge, so fix the page setup until every column shows in full and no heading is left stranded at the foot of a page.

Build the figure as `XPS_Sulfur_Region.pdf`, laid out to match `Figure_Layout_Reference.png`: Panel (a) is the sulfur region with the raw scan, the smoothed trace and the background over it, and a vertical marker at each assigned position scaled to that species' modelled height on the right-hand axis, labelled with the species. Panel (b) is the aqueous record: feed and treated effluent sulfate against elapsed days with the treated effluent zinc on the right-hand axis, and the failure day marked and called out. The species labels crowd badly around the strong doublet, so open the rendered page and move them until each one is clear of the traces and of the other labels, and check the failure-day callout is clear too. Then place the finished figure in the record where the template marks it.

Finally give me `Speciation_Summary.xlsx` with three sheets in this order: `Smoothed Spectrum`, carrying the binding energy with the raw, smoothed and background-corrected intensity, one row for every binding energy on the sheet in the order the sheet has them; `Peak Assignments`, carrying the species table; and `Column Performance`, carrying the performance table. Same values as the record. Set the two summary sheets to print on one page and the spectrum sheet to repeat its header row across pages, and format the removal columns the way we always do, with any value below zero shown in red.

Save `XPS_Analysis_Record.docx`, `XPS_Sulfur_Region.pdf` and `Speciation_Summary.xlsx` all to the Desktop when you're done.
