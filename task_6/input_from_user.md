I rewrote the prompt and rebuilt the input files around it. The original request never pinned down the analysis, so I added the smoothing window, the polynomial order, and how the ends are handled. That choice decides the whole task: at a five-point window, all six references find a local maximum. At a nine-point window, only four do, and the assignment can't be finished. For the same reason, I pinned the background as a straight line through the first and last five smoothed points, and I stated the search radius and the rule for taking the taller of two equally close maxima. Two of the references sit exactly between two peaks, so without that rule it's a coin flip. The task now uses the whole of `Bioreactor_XPS_Dataset.xlsx` instead of just one sheet. The aqueous sheets and the influent table feed a column performance summary, and the agent has to find the failure day from the treated effluent pH record instead of being told it, count entries below the reporting limit as half that limit, drop sampling days missing either side of the comparison, and read the influent values out of the "value plus or minus uncertainty" strings. Two of the removals go negative after the failure day. That's the finding the write-up has to reach, and the spectrum alone can't support it. I changed the deliverables to `XPS_Analysis_Record.docx`, `XPS_Sulfur_Region.pdf`, and `Speciation_Summary.xlsx`, because the originals were mostly plain text with nothing to look at. Most of the added difficulty is in the interface. `Analysis_Record_Template.docx` is last month's draft: track changes are still on, three edits are pending, four comments need handling, and both tables are full of last run's numbers. Its performance table is also wider than the page, so it runs off the edge until the page setup is fixed. `Figure_Layout_Reference.png` sets the two-panel layout the figure has to match, and the species labels collide around the strong doublet until they're moved on the rendered page. `Images_XPS.zip` still holds the six peak heights only as text printed on the images, so they have to be read off by hand. I also removed a researcher's name from the workbook and its metadata, corrected the Read Me, and repacked the image archive without its embedded text chunks.

Second pass, after review. I merged two duplicate table-legibility rubric criteria into one to free up a slot, and added a criterion that checks the finished figure is actually embedded in `XPS_Analysis_Record.docx` at the spot the template marks. No existing criterion tested for that, so a response that left the placeholder text in place would have passed everything else. I reworded the stranded-heading criterion to be positive, since the prompt doesn't ask to omit anything there. I added a legibility clause to the print-on-one-page criterion so it can't be satisfied by shrinking the sheet until it's unreadable. In the prompt, I reworded the reduced-to-oxidised height question so it no longer assumes which species is larger, and reworded the red-formatting instruction so it reads as a standing convention instead of a hint that some removal values come out negative. I also moved the non-load-bearing procedural detail (the background-line definition, the peak-matching search radius and tie-break rule, the half-limit convention, and the rounding precision) out of the prompt and into a new reference file, `XPS_Processing_Notes.pdf`. Together they were reading more like a spec than a request. I kept the five-point smoothing window and the failure-day definition in the prompt, since those are the task-specific judgment calls that actually decide the answer. I still need to fix the embedded figure's size in `XPS_Analysis_Record.docx`. It's carried over from the original portrait layout and is now too small for the landscape page.

Third pass, after a second review of the new reference file. I fixed a real bug in `XPS_Processing_Notes.pdf`: the matching step said to use the background-corrected trace instead of the smoothed trace, which disagreed with both the prompt and the expected files. I rewrote the end-reflection and background-line rules precisely: the reflection mirrors the trace without repeating the boundary point, and the background line runs through the mean binding energy and the mean intensity of the first and last five smoothed points, not just the mean intensity. I re-derived the smoothing, background, and peak-matching from the raw `Fig 3` data under each possible reading, and confirmed which one reproduces the expected files' numbers exactly, rather than taking the review's word for it. I also fixed real ambiguities in the prompt. "Disagree most" now says "in absolute terms," since I confirmed against the expected file that the comparison is absolute, not relative. The ratio question now says "combined modelled peak height," since the expected file sums each group's components rather than averaging them. The removal question now says "calculate each period's removal from those means," since the expected file computes removal from the period means, not from daily removals averaged together. The difference question now specifies "amplitude minus modelled height." I added the explicit line telling the agent to save outputs to the Desktop, which was missing from every earlier version of this prompt. I cut the repeated citations of `XPS_Processing_Notes.pdf` from five down to two named mentions plus two natural references to the same file, since naming it in every paragraph read mechanically. I declined a few other suggested changes: the panel (a)/(b) figure description and the "open the rendered page and move them" instruction both match this project's own guidelines and worked examples, so I left them as written.

Fourth pass. I caught an inconsistency in my own third-pass fix. I had moved the processing conventions (background, search radius, tie-break, rounding) into `XPS_Processing_Notes.pdf`, but I bolted the reporting disambiguations (subtraction order, absolute versus relative, sum versus average) directly onto the prompt instead of giving them the same treatment. That pushed the prompt back toward reading like a spec. I moved all three into a new "Reporting conventions" section in the notes file instead, and reverted the prompt's conclusions clause to its plain wording. I also brought the file citations down from five mentions across four paragraphs to one blanket reference where the file is introduced, and trimmed the redundant "the way we always do... [X]" framing on the red-formatting line to a single clause.
