I rewrote the prompt and rebuilt the input files around it. The original request never pinned the analysis, so I stated the smoothing window, the polynomial order and how the ends are handled, because that choice decides the whole task: at a five-point window all six references find a local maximum, at nine only four do and the assignment cannot be finished. For the same reason I pinned the background as a straight line through the first and last five smoothed points, and I stated the search radius and the rule for taking the taller of two equally close maxima, since two of the references sit exactly between two peaks and are otherwise a coin flip. The task now works from the whole of `Bioreactor_XPS_Dataset.xlsx` instead of one sheet: the aqueous sheets and the influent table feed a column performance summary where the agent has to find the failure day from the treated effluent pH record rather than being told it, count entries below the reporting limit as half that limit, drop sampling days missing either side of the comparison, and read the influent values out of the "value plus or minus uncertainty" strings. Two of the removals go negative after that day, which is the finding the write-up has to reach and which the spectrum on its own cannot support. I changed the deliverables to `XPS_Analysis_Record.docx`, `XPS_Sulfur_Region.pdf` and `Speciation_Summary.xlsx`, because the originals were mostly plain text and carried nothing to look at. Most of the added difficulty is in the interface: `Analysis_Record_Template.docx` is last month's draft with track changes still switched on, three edits pending, four comments to deal with and both tables full of the previous run's numbers, and its performance table is set wider than the page so it visibly runs off the edge until the page setup is fixed. `Figure_Layout_Reference.png` fixes the two-panel layout the figure has to match, and the species labels collide around the strong doublet until they are moved on the rendered page. `Images_XPS.zip` still holds the six peak heights only as text printed on the images, so they have to be read off. I also took a researcher's name out of the workbook and its metadata, corrected the Read Me, and repacked the image archive without its embedded text chunks.

Second pass, after review: merged two duplicate table-legibility rubric criteria into one to free a slot, and added a criterion checking that the finished figure is actually embedded in `XPS_Analysis_Record.docx` at the spot the template marks it, since no existing criterion tested for that and a response that left the placeholder text in place would have passed everything else. Reworded the stranded-heading criterion positively, since the prompt doesn't ask to omit anything there. Added a legibility clause to the print-on-one-page criterion so it can't be satisfied by shrinking the sheet to unreadable size. In the prompt, reworded the reduced-to-oxidised height ask so it no longer presupposes which species is larger, and reworded the red-formatting instruction so it reads as a standing convention rather than a hint that some removal values come out negative. Also moved the non-load-bearing procedural detail (background-line definition, peak-matching search radius and tie-break, half-limit convention, rounding precision) out of the prompt and into a new reference file, `XPS_Processing_Notes.pdf`, since together they were reading more like a spec than a request; kept the five-point smoothing window and the failure-day definition inline since those are the task-specific judgment calls that actually decide the answer. Still need to fix the embedded figure's size in `XPS_Analysis_Record.docx` — it's carried over from the original portrait layout and is now undersized for the landscape page.

Third pass, after a second review of the new reference file: fixed a real bug in `XPS_Processing_Notes.pdf` where the matching step said to use the background-corrected trace instead of the smoothed trace, which disagreed with both the prompt and the expected files. Rewrote the end-reflection and background-line bullets precisely (mirror without repeating the boundary point; background line through the mean binding energy AND mean intensity of the first/last five smoothed points, not just mean intensity) — I re-derived the smoothing, background and peak-matching from the raw `Fig 3` data under each candidate reading and confirmed which one reproduces the expected files' numbers exactly before writing the fix, rather than taking the review's word for it. Also fixed real ambiguities in the prompt: "disagree most" now says "in absolute terms" (confirmed against the expected file that this is absolute, not relative, difference), the ratio ask now says "combined modelled peak height" (confirmed the expected file sums each group's components rather than averaging them), the removal ask now says "calculate each period's removal from those means" (confirmed the expected file computes removal from the period means, not from daily removals averaged together), and the difference ask now specifies "amplitude minus modelled height". Added the explicit Desktop-save line for outputs, which was missing throughout every prior version of this prompt. Cut the repeated `XPS_Processing_Notes.pdf` citations from five down to two named mentions plus two natural references to the same file, since citing it by name in every paragraph read mechanically. Declined a few other suggested changes: the panel (a)/(b) figure description and the "open the rendered page and move them" GUI instruction both match this project's own guidelines and worked examples, so left those as written.
