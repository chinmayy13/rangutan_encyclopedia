The seed prompt didn't pin the analysis, so I added the smoothing window, the polynomial order, and how the ends are handled, since that choice decides which references resolve. I pinned the background line and the peak-matching rule for the same reason, since two references sit right between two peaks. The prompt now pulls from the whole workbook instead of one sheet, so the agent has to find the failure day itself, halve any below-detection reading, drop incomplete days, and read values out of the "value ± uncertainty" strings. I named the deliverables `XPS_Analysis_Record.docx`, `XPS_Sulfur_Region.pdf`, and `Speciation_Summary.xlsx`.

After review, I fixed two lines that leaked the answer: the ratio question no longer implies which species is bigger, and the red-formatting line no longer hints that some removals come out negative. I moved the non-essential method detail (background, search radius, tie-break, half-limit, rounding) out of the prompt and into a new file, `XPS_Processing_Notes.pdf`, but kept the window and the failure-day rule in the prompt since those actually decide the answer. I added the missing line telling the agent to save outputs to the Desktop. I briefly added extra clarifying words to the conclusions line, then took them back out since they made the prompt read like a spec, and moved them into the notes file instead. I also cut repeated mentions of `XPS_Processing_Notes.pdf` from five down to one, trimmed the red-formatting line to a single clause, and removed the em-dashes.
