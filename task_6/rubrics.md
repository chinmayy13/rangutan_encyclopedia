Write criteria that encompass all requirements needed to fulfill this prompt.
30/30 completed

Edit raw criteria

Show summary

List view
Search criteria...
1
Output includes a file named `XPS_Analysis_Record.docx` that opens as a valid document in .docx format.
5 points · MUST-PASS · format_gate

2
Output includes a file named `XPS_Sulfur_Region.pdf` that opens as a valid PDF.
5 points · MUST-PASS · format_gate

3
Output includes a file named `Speciation_Summary.xlsx` that opens as a valid workbook in .xlsx format.
5 points · MUST-PASS · format_gate

4
`XPS_Analysis_Record.docx` opens with no tracked change left pending and no review comment left in the document, as in the corresponding expected file.
12 points · REGULAR · visual

5
`Speciation_Summary.xlsx` contains the same three named sheets, in the same order, as the corresponding expected file.
12 points · REGULAR · format_gate

6
The column headings of the assignment table in `XPS_Analysis_Record.docx` identify the same fields and units as the corresponding expected file, distinguishing the modelled peak height from the measured amplitude.
12 points · REGULAR · correctness

7
The method section of `XPS_Analysis_Record.docx` describes the same smoothing settings, background treatment, marker matching rule, failure-day split, half-limit substitution for censored entries and exclusion of sampling days missing either paired value as the corresponding expected file.
24 points · REGULAR · correctness

8
The assignment table in `XPS_Analysis_Record.docx` carries exactly one row for every reference species in the corresponding expected file, and for each of them the assigned binding energy, offset, measured amplitude, modelled peak height and difference match that species' values in the corresponding expected file within ±1 (positions and offsets) and ±0.5 counts/s (amplitude, modelled height and difference), with any species recorded as not resolved treated the same way.
40 points · REGULAR · correctness

9
The performance table in `XPS_Analysis_Record.docx` carries a row for every analyte in the corresponding expected file, and for each of them the independently characterised influent value and the mean influent, mean treated effluent and removal for both the operating period and the period after the failure day match that analyte's values in the corresponding expected file within ±1%.
40 points · REGULAR · correctness

10
The conclusions section of `XPS_Analysis_Record.docx` reports the same largest offset and species, the same species with the largest gap between modelled height and measured amplitude, the same reduced-to-oxidised sulfur height ratio and the same account of the metals after the failure day as the corresponding expected file.
30 points · REGULAR · correctness

11
Panel (a) of `XPS_Sulfur_Region.pdf` carries a vertical marker and a species label for every resolved assignment in the corresponding expected file, each marker placed at that species' assigned binding energy and scaled to its modelled peak height on the right-hand axis.
26 points · REGULAR · correctness

12
Panel (b) of `XPS_Sulfur_Region.pdf` plots the same influent sulfate, treated effluent sulfate and treated effluent zinc records against elapsed time as the corresponding expected file, with the zinc record read against the right-hand axis and the failure day marked at the same day.
20 points · REGULAR · correctness

13
On the spectrum sheet of `Speciation_Summary.xlsx`, every binding energy, raw intensity, smoothed intensity and background-corrected intensity matches the corresponding expected file within +/-0.05 counts/s.
30 points · REGULAR · correctness

14
The species rows on the peak assignment sheet of `Speciation_Summary.xlsx` carry the same values as the assignment table of `XPS_Analysis_Record.docx`, matching within ±1 (positions and offsets) and ±0.5 counts/s (amplitude, modelled height and difference).
10 points · REGULAR · correctness

15
The analyte rows on the column performance sheet of `Speciation_Summary.xlsx` carry the same values as the performance table of `XPS_Analysis_Record.docx`, matching within ±1%.
10 points · REGULAR · correctness

16
The binding-energy axis of panel (a) in `XPS_Sulfur_Region.pdf` runs from high to low over the same range as the corresponding expected file.
8 points · REGULAR · visual

17
The elapsed-time axis of panel (b) in `XPS_Sulfur_Region.pdf` runs in the same direction over the same range as the corresponding expected file.
6 points · REGULAR · visual

18
Panel (a) of `XPS_Sulfur_Region.pdf` carries both vertical axes, each with its own label and its own tick values, matching the corresponding expected file.
8 points · REGULAR · visual

19
Panel (b) of `XPS_Sulfur_Region.pdf` carries both vertical axes, each with its own label and its own tick values, matching the corresponding expected file.
8 points · REGULAR · visual

20
The spectrum sheet of `Speciation_Summary.xlsx` lists its rows in the same order as the corresponding expected file.
6 points · REGULAR · visual

21
The panel tags, the panel titles, the legends and the caption of `XPS_Sulfur_Region.pdf` sit in the same positions as the corresponding expected file.
8 points · REGULAR · visual

22
In `XPS_Analysis_Record.docx`, no section heading is left stranded at the foot of a page as in the corresponding expected file.
6 points · REGULAR · visual

23
Every species label in panel (a) of `XPS_Sulfur_Region.pdf` is fully legible and clear of the plotted traces and of the other labels.
10 points · REGULAR · visual

24
The failure-day callout in panel (b) of `XPS_Sulfur_Region.pdf` is fully legible and clear of the plotted series, matching the corresponding expected file.
6 points · REGULAR · visual

25
The assignment table in `XPS_Analysis_Record.docx` shows every column in full on the page with all cell text legible at normal viewing size, matching the corresponding expected file.
6 points · REGULAR · visual

26
The performance table in `XPS_Analysis_Record.docx` shows every column in full on the page with all cell text legible at normal viewing size, matching the corresponding expected file.
6 points · REGULAR · visual

27
Panel (a) of `XPS_Sulfur_Region.pdf` draws the raw scan, the smoothed trace and the background, distinguishable from one another, matching the corresponding expected file.
6 points · REGULAR · visual

28
The peak assignment and column performance sheets of `Speciation_Summary.xlsx` are set to print on a single page.
5 points · REGULAR · format_gate

29
The spectrum sheet of `Speciation_Summary.xlsx` repeats its header row across printed pages, matching the corresponding expected file.
5 points · REGULAR · visual

30
Every negative removal value in `Speciation_Summary.xlsx` is shown in red, matching the corresponding expected file.
6 points · REGULAR · visual
