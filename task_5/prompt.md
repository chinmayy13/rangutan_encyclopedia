Hey, we've been going back and forth on whether react-flow + roughjs + dagre is enough for the agent canvas work, or whether we need to bring in elkjs and d3-hierarchy too, and I'd like this settled with actual evidence instead of another meeting. I've left everything on the Desktop.

We've got two runs of the spatial benchmark (`agent_spatial_benchmark_run1.csv` and `agent_spatial_benchmark_run2.csv`) that are giving us conflicting accuracy numbers for the same four stacks, and two sets of capability notes from our assessors (`capability_notes_source_a.md` and `capability_notes_source_b.md`) that flatly disagree on whether roughjs exposes bounding-box metadata natively and whether elkjs is actually needed. There's also a draft analysis script (`canvas_benchmark_analysis.R`) if it's a useful starting point, use it, extend it, or work however's fastest for you, plus a half-finished recommendation (recommendation_draft.md) that has the framing but no verdict in it yet.

Can you pull this into one clear evidence pack? Build a grouped bar chart comparing both runs across all four stacks and metrics, and call out anywhere the two runs diverge by more than 15 points. I want to see it visually, not buried in a table, and I want to be sure the annotations are actually legible and not overlapping once it's rendered, before you save it. Save that as benchmark_comparison_annotated.png on the Desktop. Then reconcile the two conflicting sets of capability notes against what the benchmark actually shows, and write that up as `library_delta_table.md` on the Desktop. I want to know where each disagreement comes from and which source the evidence actually backs. Finally, pull it all together into one final writeup, canvas_agent_evidence_pack.md, saved to the Desktop as well. Build on the existing draft in recommendation_draft.md where it's useful, embed the chart and the delta table, and give me a clear recommendation on whether the base stack is enough, or whether we need elkjs and d3-hierarchy for agents to reliably know where a node sits, which page it's on, and what it depends on. The whole point here is that agents need to be able to autonomously figure out where to insert new nodes, which page to open, and what depends on what, so make sure the recommendation speaks directly to that.
