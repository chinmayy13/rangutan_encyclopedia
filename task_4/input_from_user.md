I substantially revised the prompt, including changing the input-file set, as explained below.

1. Changed the inherited input-file set: The initial input set included Supertrapp.pdf, CH4 (1).pdf, and C2H4 (1).pdf. These NIST/SUPERTRAPP materials did not meet the project's starter-file licensing requirement, which requires files that are freely usable with no attribution or other restrictive licensing terms. I therefore excluded these files from the revised task. The interface does not allow the inherited attachments to be deleted, so instead of adding a prompt instruction telling the agent to avoid them, they are simply left out of the Task Initializer's file list so the agent never receives them.

2. Added three finalized replacement inputs: I added CH4_C2H4_Component_Reference.docx, FUG_inputs.pdf, and CH4_C2H4_FUG_Calculation_Record_Template.docx. These files now provide the component data, case data, calculation conventions, and calculation-record structure needed for the task.

3. Rewrote the prompt to address the previous reviewer feedback: I removed the overly prescriptive slide-by-slide and memo instructions and made the request more natural. The revised prompt keeps the engineering objective, required deliverables, reporting precision, and the information the project review board needs.

4. Improved technical clarity, complexity, and GUI-related requirements: The revised task requires the agent to integrate all three starter files, perform the molar balance and Fenske-Underwood-Gilliland calculations, verify the Underwood result, and present the results in three professional deliverables. It also requires the calculation record to preserve the supplied template and render cleanly, the memo to fit on one readable page, and the presentation to have clear visual hierarchy with no clipping, overlap, or truncation.

5. Increased task difficulty in response to review feedback (agent scored 81.66% across three runs, above the 65% target): the relative volatility was changed from a single supplied constant (alpha = 1.85) to two endpoint values, alpha_top = 2.500 (distillate-end conditions) and alpha_bottom = 1.369 (bottoms-end conditions), given in the revised FUG_inputs.pdf. The agent must recognize that the shortcut method requires a single representative value and apply the standard Fenske convention of taking the geometric mean of the two endpoint values, which works out to the original 1.85 by construction, so every other reported value in the calculation record, memo, and deck is unchanged. The calculation-record template's own instructional brackets previously stated "alpha_LK = 1.85" directly, which handed the answer to the agent regardless of the case data; that hardcoded value was removed from the template so the agent must actually perform the derivation. No prompt, GTF filename, or rubric criterion changes were needed: the existing anchored numeric criteria (case-basis, Fenske, Underwood, Gilliland, and summary-table checks) already fail an agent that derives the wrong effective alpha, since every downstream value depends on it.
